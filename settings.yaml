# Airflow version to be installed
airflow_version: "2.7.3"

# Python version for the project
python_version: "3.10.12"

# Path to a virtual evn
mode:
  virtualenv:
    venv_path: "/home/grayson/project-folder/superflex_lines/.venv"

# Airflow connections
connections:
    # Example connection
    # - conn_id: example
    #   conn_type: http
    #   host: http://example.com
    #   port: 80
    #   login: user
    #   password: pass
    #   schema: http
    #   extra:
    #      example_extra_field: example-value

# Airflow variables
variables:
    # Example variable
    # - key: example
    #   value: example-value
    #   description: example-description